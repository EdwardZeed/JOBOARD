# Anthropic API key for the Agent SDK (this is real per-token API billing,
# not covered by a Claude subscription — see docs/routine-prompt.md).
ANTHROPIC_API_KEY=sk-ant-...

# Same values as apps/web's MCP_AUTH_TOKEN / the deployed /api/mcp URL.
MCP_URL=https://web-pink-two-30.vercel.app/api/mcp
MCP_TOKEN=your-mcp-auth-token
